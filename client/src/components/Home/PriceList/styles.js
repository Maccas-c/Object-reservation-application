import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  main: {
    margin: '32px 300px !important',
  },
  title: {
    fontWeight: 700,
    display: 'flex',
    marginLeft: '20px',
  },
}));

export default useStyles;
