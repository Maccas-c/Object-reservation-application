import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    marginTop: '50px',
    minWidth: 258,
  },
  title: {
    fontSize: 14,
  },
}));

export default useStyles;
