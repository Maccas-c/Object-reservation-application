import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  table: {
    width: '70%',
    marginLeft: '15%',
  },
  betweenTable: {
    marginTop: '30px',
  }
}));

export default useStyles;
